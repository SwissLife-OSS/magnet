module.exports = {
  schema: "./schema.graphql",
  src: "src",
  eagerEsModules: false,
  noFutureProofEnums: true,
  language: "typescript",
  // Remove persistConfig to disable persisted queries
};
